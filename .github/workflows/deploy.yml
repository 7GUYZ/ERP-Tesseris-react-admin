<<<<<<< HEAD
name: Deploy to Kubernetes
=======
name: Deploy React App to Kubernetes
>>>>>>> dev

on:
  push:
    branches:
<<<<<<< HEAD
      - sichan  # 또는 배포 브랜치명
=======
      - main
      - dev
>>>>>>> dev

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
<<<<<<< HEAD
=======
    env:
      IMAGE_NAME: ${{ secrets.DOCKERHUB_USERNAME }}/react-admin:latest
>>>>>>> dev
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
<<<<<<< HEAD
        uses: docker/setup-buildx-action@v2

      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USER }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_IMAGE }}:${{ github.sha }}

      - name: Set up Kubeconfig
        uses: azure/setup-kubectl@v4
=======
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ env.IMAGE_NAME }}

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
>>>>>>> dev
        with:
          version: 'latest'

      - name: Configure Kubeconfig
        run: |
<<<<<<< HEAD
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBE_CONFIG }}" > $HOME/.kube/config

      - name: Replace image tag and apply deployment
        run: |
          sed "s#__DOCKER_IMAGE__#${{ secrets.DOCKER_IMAGE }}#g" k8s/deployment.yaml | \
          sed "s#__TAG__#${{ github.sha }}#g" > k8s/deployment.generated.yaml

          kubectl apply -f k8s/deployment.generated.yaml

      - name: Apply service
        run: |
          kubectl apply -f k8s/service.yaml
=======
          echo "${{ secrets.KUBE_CONFIG_DATA }}" | base64 -d > $HOME/.kube/config

      - name: Deploy to Kubernetes
        run: |
          kubectl apply -f my-k8s-app/final/reactadmin_deployment.yaml
          kubectl apply -f my-k8s-app/final/reactadmin_service.yaml 
>>>>>>> dev
